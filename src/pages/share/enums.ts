export enum status {

    notUpdate = 1,
    update = 2,

}
//check status enum
export enum checkStatus {

    notPaid = 0,
    paid = 1,
    returened = 2,

}
export enum openOption {
    paid = 2,
    notPaid = 3,
    pastDueDate = 4,
}
export enum idOrPassport {
    id = 0,
    passport = 1,
    
}